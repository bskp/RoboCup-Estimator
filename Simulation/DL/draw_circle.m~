function draw_circle(Pos_x,Pos_y,r,color)
%DRAW_CIRCLE

xl = xlim;      % Originalgrösse merken
yl = ylim;

phi=linspace(0,2*pi);
x=cos(phi);
y=sin(phi);

plot(Pos_x + r*x,Pos_y + r*y,color);

xlim(xl);
ylim(yl);

end

